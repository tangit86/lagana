package my.than.lagana.core.bake.conf;

import my.than.lagana.core.common.LaganaConfiguration;

import java.io.IOException;

public class BakeConfiguration extends LaganaConfiguration {

    public BakeConfiguration(String appConfPath) throws IOException {
        super(appConfPath);
    }
}
